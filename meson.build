project('clapper-vala-test', ['c', 'vala'],
  version: '0.0.1',
  meson_version: '>= 0.58.0',
  default_options: [
    'warning_level=1',
    'buildtype=debugoptimized',
  ],
)

gnome = import('gnome')

subdir('src')
